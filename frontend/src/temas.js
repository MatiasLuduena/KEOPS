// cambiar color
import { createTheme } from '@mui/material/styles';

export const temas = createTheme({
  palette: {
    primary: {
      main: "#27ae60"
    },
    text: {
      main: "#fff"
    }
  }
});